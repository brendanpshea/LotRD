[
  {
    "question": "Which security goal in the CIA triad ensures that data is accessible only to authorized people?",
    "correct": [
      "Confidentiality"
    ],
    "incorrect": [
      "Integrity",
      "Availability",
      "Authenticity"
    ],
    "feedback": "Confidentiality guarantees that private information is protected from unauthorized access or disclosure."
  },
  {
    "question": "A clinic's patient records are read by outsiders who typed something clever into a login box. Which property failed?",
    "correct": [
      "Confidentiality"
    ],
    "incorrect": [
      "Integrity",
      "Availability",
      "Compatibility"
    ],
    "feedback": "Nobody changed the records and the portal stayed up — outsiders simply read data they should not have. Altering a prescription would break integrity; knocking the portal offline would break availability."
  },
  {
    "question": "Which goal in the CIA triad ensures that data cannot be unauthorizedly modified or corrupted?",
    "correct": [
      "Integrity"
    ],
    "incorrect": [
      "Confidentiality",
      "Availability",
      "Anonymity"
    ],
    "feedback": "Integrity guarantees that data remains accurate and trustworthy, protected against unauthorized modification."
  },
  {
    "question": "Which goal in the CIA triad ensures that systems and services remain operational when needed?",
    "correct": [
      "Availability"
    ],
    "incorrect": [
      "Confidentiality",
      "Integrity",
      "Non-repudiation"
    ],
    "feedback": "Availability ensures that authorized users have timely, reliable access to computing systems and data."
  },
  {
    "type": "npc_demo",
    "question": "NPC: The quote that opened the gate",
    "npc": "Ada the Artificer",
    "intro": "Ada stops at a gatehouse where a clerk copies every visitor's name into a ledger query. “This gate has stood for years. I am going to walk through it without a key, and then show you the only fix that actually works.”",
    "steps": [
      {
        "say": "The clerk builds the lookup by pasting text together:\nquery = f\"SELECT * FROM users WHERE name = '{username}'\"\nI give the name mina, and the database receives WHERE name = 'mina'. Perfectly fine — which is exactly why nobody notices the flaw.",
        "beats": [
          {
            "say": "The clerk builds the lookup by pasting text together:"
          },
          {
            "code": "query = f\"SELECT * FROM users WHERE name = '{username}'\"",
            "language": "python"
          },
          {
            "say": "I give the name mina, and the database receives WHERE name = 'mina'. Perfectly fine — which is exactly why nobody notices the flaw."
          }
        ]
      },
      {
        "say": "Now I hand over a stranger name: ' OR '1'='1\nThe clerk pastes it in as always, and the database receives WHERE name = '' OR '1'='1'.",
        "check": {
          "prompt": "What does the database do with that condition?",
          "answer": "It is true for every row, so every user matches",
          "wrong": [
            "It looks for a user literally named ' OR '1'='1",
            "It reports a syntax error and runs nothing"
          ],
          "why": "My quote closed the name string early, so everything after it was read as SQL code rather than as a name. '1'='1' is always true."
        },
        "beats": [
          {
            "say": "Now I hand over a stranger name:"
          },
          {
            "code": "' OR '1'='1",
            "language": "typed by the attacker"
          },
          {
            "say": "The clerk pastes it in as always, and the database receives WHERE name = '' OR '1'='1'."
          }
        ]
      },
      {
        "say": "The fix is not to hunt for dangerous characters — attackers always find another spelling. The fix is to stop mixing code and data at all. A parameterized query sends the skeleton and the value down separate channels:\ncursor.execute(\"SELECT * FROM users WHERE name = ?\", (username,))\nNow that same input is looked up as a literal name, matches nobody, and returns nothing.",
        "beats": [
          {
            "say": "The fix is not to hunt for dangerous characters — attackers always find another spelling. The fix is to stop mixing code and data at all."
          },
          {
            "say": "A parameterized query sends the skeleton and the value down separate channels:"
          },
          {
            "code": "cursor.execute(\"SELECT * FROM users WHERE name = ?\", (username,))",
            "language": "python"
          },
          {
            "say": "Now that same input is looked up as a literal name, matches nobody, and returns nothing."
          }
        ]
      }
    ],
    "outro": "“Name the fix, and remember why the clever character-filter is not it.”"
  },
  {
    "question": "A login function builds its query as  f\"SELECT * FROM users WHERE name = '{username}'\"  and someone types  ' OR '1'='1  as the username. What does the database receive?",
    "correct": [
      "A condition that is always true, so every user row is returned"
    ],
    "incorrect": [
      "A syntax error, because the quotes do not balance",
      "A request for the single user actually named ' OR '1'='1",
      "An empty result, because no such name exists"
    ],
    "feedback": "The attacker's quote closed the string early and their OR clause bolted on a condition that is always true. Scale three rows to forty thousand patient records and you have the morning that started the notebook."
  },
  {
    "question": "How does a SQL injection attack work?",
    "correct": [
      "Smuggling malicious SQL commands into a database query through unsanitized user input"
    ],
    "incorrect": [
      "Injecting script tags into stored comments so they run inside another user's browser",
      "Tricking a logged-in user's browser into sending an unwanted request to a trusted site",
      "Guessing account passwords repeatedly using an automated dictionary of likely choices"
    ],
    "feedback": "SQL injection occurs when unsanitized user input is concatenated directly into SQL query strings, allowing attackers to run arbitrary SQL commands."
  },
  {
    "question": "What is the primary method used to prevent SQL injection vulnerabilities in application code?",
    "correct": [
      "Using parameterized queries so input is treated strictly as data rather than SQL code"
    ],
    "incorrect": [
      "Using client-side JavaScript to block quote characters before the form is submitted",
      "Using a hash of each submitted field so stored values cannot be read by an attacker",
      "Using HTTPS so the query cannot be modified while it travels toward the database"
    ],
    "feedback": "Parameterized queries separate the SQL code structure from user parameters (using placeholders like `?`), rendering injected SQL harmless."
  },
  {
    "type": "fill_blank",
    "question": "The three components of the CIA security triad are confidentiality, availability, and ___.",
    "correct": [
      "integrity"
    ],
    "case_sensitive": false,
    "feedback": "The CIA triad stands for Confidentiality, Integrity, and Availability."
  },
  {
    "question": "With a parameterized query in place, what happens when someone submits  ' OR '1'='1  as a username?",
    "correct": [
      "It is looked up as a literal name, finds nobody, returns nothing"
    ],
    "incorrect": [
      "The database rejects the request with a syntax error",
      "The characters are stripped out and the rest of the name is used",
      "Every user row is returned, but the login is refused afterwards"
    ],
    "feedback": "The whole string is treated as data. Since no user is actually called that, the attack simply fails — and ordinary logins keep working exactly as before."
  },
  {
    "type": "fill_blank",
    "question": "An attack where malicious JavaScript input is executed inside another user's web browser is known as ___.",
    "correct": [
      "XSS",
      "cross-site scripting",
      "cross site scripting"
    ],
    "case_sensitive": false,
    "feedback": "XSS (Cross-Site Scripting) occurs when untrusted user input is rendered in a browser as executable script code."
  },
  {
    "type": "fill_blank",
    "question": "A one-way mathematical function that converts input text into a fixed-length fingerprint for password storage is a ___.",
    "correct": [
      "hash",
      "hash function",
      "hashing function"
    ],
    "case_sensitive": false,
    "feedback": "Hashing is a one-way transformation used to verify passwords without storing plaintext credentials."
  },
  {
    "type": "dynamic_numeric",
    "question": "A hash function produces a digest of {{bits}} bits. Written out in hexadecimal, where each character stands for 4 bits, how many characters long is the digest?",
    "variables": {
      "bits": {
        "values": [
          128,
          160,
          256,
          512
        ]
      }
    },
    "answer": {
      "expr": "bits / 4",
      "tolerance_abs": 0
    },
    "feedback_template": "Each hex character encodes 4 bits, so {{bits}} bits print as {{expected}} characters. The length is fixed by the algorithm, not by the password, which is why a hash never reveals how long the original input was."
  },
  {
    "question": "Two users of a properly salted password database happen to choose the very same password. What do their two stored hash values look like?",
    "correct": [
      "Different, because each password is hashed together with its own random salt"
    ],
    "incorrect": [
      "Identical, because a hash function always maps one input to one fixed output value",
      "Different, because the hash function mixes in fresh randomness on every single call",
      "Identical, because the salt is attached after hashing and only pads the stored length"
    ],
    "feedback": "The hash function itself is deterministic, so without salts the two records would match and reveal the shared password at a glance. The randomness lives in the per-user salt that is combined with the password before hashing, not inside the algorithm."
  },
  {
    "type": "fill_blank",
    "question": "Random data mixed into a password before hashing to defeat precomputed rainbow table attacks is called a ___.",
    "correct": [
      "salt"
    ],
    "case_sensitive": false,
    "feedback": "A salt is a unique, random string appended to a password before hashing to prevent rainbow table dictionary attacks."
  },
  {
    "type": "cloze",
    "question": "Three steps that look alike: storing a password uses {{1}}, which is one-way and cannot be undone; mixing a fresh random value into each password before that step is called {{2}}; and protecting a message in transit uses {{3}}, which is deliberately reversible by someone holding the right key.",
    "blanks": [
      {
        "accept": [
          "hashing",
          "a hash",
          "hash"
        ],
        "hint": "one-way"
      },
      {
        "accept": [
          "salting",
          "salt"
        ],
        "hint": "random value"
      },
      {
        "accept": [
          "encryption",
          "encrypting"
        ],
        "hint": "reversible"
      }
    ],
    "feedback": "Hashing is one-way, so a site that can email you your password is not hashing it. Salting makes two identical passwords hash differently, defeating precomputed rainbow tables. Encryption is the only one of the three meant to be undone — by whoever holds the key."
  },
  {
    "type": "ordering",
    "question": "Arrange what a secure system does, in order, when a user sets a new password.",
    "items": [
      "Receive the password the user typed",
      "Generate a fresh random salt for this user",
      "Combine that salt with the password and hash the result",
      "Store the resulting hash together with its salt"
    ],
    "feedback": "Each step needs the one before it: there is nothing to salt until the password arrives, nothing to hash until the salt exists, and nothing to store until the hash is computed. The plain password is never written down at all."
  },
  {
    "question": "A patient portal shows staff the comments patients leave. An attacker submits a comment containing a script tag. What is the danger?",
    "correct": [
      "Dropped straight onto the page, the script runs in the staff member's browser"
    ],
    "incorrect": [
      "The comment crashes the server at the moment it is written to storage",
      "The comment overwrites the other comments already held in the database",
      "The comment makes the page load noticeably more slowly for every visitor"
    ],
    "feedback": "This is cross-site scripting. The victim's browser cannot tell the attacker's script from the site's own code, so it obeys it — in someone else's session."
  },
  {
    "question": "Which of the following statements correctly distinguish hashing from encryption? (Select all that apply)",
    "correct": [
      "Hashing is a one-way transformation that cannot be mathematically reversed",
      "Encryption is a two-way process that can be decrypted using a secret key"
    ],
    "incorrect": [
      "Hashing allows developers to decrypt passwords back into original plaintext",
      "Encryption is used exclusively for password storage and never for data transit"
    ],
    "feedback": "Hashing is a one-way fingerprint (irreversible), whereas encryption is a two-way process (reversible with a key)."
  },
  {
    "question": "Which of the following are effective practices for keeping API keys and secrets safe? (Select all that apply)",
    "correct": [
      "Storing secrets in environment variables outside the application source code",
      "Adding secret files and configuration paths to the project's .gitignore file"
    ],
    "incorrect": [
      "Hardcoding secret credentials directly into public GitHub repository files",
      "Embedding private API keys directly in public HTML and JavaScript web templates"
    ],
    "feedback": "Secrets must never be hardcoded or committed to git; store them in environment variables and exclude secret files via `.gitignore`."
  },
  {
    "type": "code_line",
    "question": "Using the already-imported os module, write the Python expression that reads the environment variable named KEY and returns None if it has not been set.",
    "language": "python",
    "correct": [
      "os.getenv('KEY')",
      "os.getenv(\"KEY\")"
    ],
    "case_sensitive": true,
    "feedback": "Reading the secret from the environment at run time means the value never appears in the source file, so the code can be shared or committed safely. Returning None instead of raising lets the program report a clear \"secret not configured\" message."
  },
  {
    "question": "Which of the following measures help protect web applications against Cross-Site Scripting (XSS)? (Select all that apply)",
    "correct": [
      "Validating user input for expected format, length, and content before processing",
      "Escaping dangerous HTML characters (<, >, &, \") before rendering input in templates"
    ],
    "incorrect": [
      "Using SQL string concatenation when inserting user text into database tables",
      "Storing user passwords in clear plaintext inside local browser storage cookies"
    ],
    "feedback": "XSS defense relies on strict input validation and escaping special HTML characters before rendering untrusted user data."
  },
  {
    "type": "matching",
    "question": "Match each security concept to its description.",
    "pairs": [
      {
        "term": "CIA Triad",
        "definition": "The core model covering confidentiality, integrity, and availability"
      },
      {
        "term": "Threat Model",
        "definition": "A structured list of system assets, potential threats, and risks"
      },
      {
        "term": "Defense in Depth",
        "definition": "Stacking multiple independent security layers for protection"
      },
      {
        "term": "Brute-Force Attack",
        "definition": "Systematically testing every possible password key until one works"
      }
    ],
    "feedback": "The CIA triad defines core security goals, threat models identify risks, defense in depth stacks security layers, and brute-force tests every key."
  },
  {
    "type": "matching",
    "question": "Match each security tool or technique to the vulnerability it mitigates.",
    "pairs": [
      {
        "term": "Parameterized Query",
        "definition": "Prevents SQL injection by isolating user parameters from SQL commands"
      },
      {
        "term": "HTML Escaping",
        "definition": "Prevents Cross-Site Scripting (XSS) by neutralizing script tags"
      },
      {
        "term": "Password Salting",
        "definition": "Defeats precomputed rainbow table attacks against stored hashes"
      },
      {
        "term": ".gitignore File",
        "definition": "Prevents sensitive secret keys from being pushed to source repositories"
      }
    ],
    "feedback": "Parameterized queries block SQLi, HTML escaping blocks XSS, salting blocks rainbow tables, and `.gitignore` protects repository secrets."
  },
  {
    "type": "dynamic_numeric",
    "question": "A PIN code has {{digits}} decimal digits. How many total possible PIN combinations exist for a brute-force attack to test?",
    "variables": {
      "digits": {
        "min": 3,
        "max": 6
      }
    },
    "answer": {
      "expr": "pow(10, digits)",
      "tolerance_abs": 0
    },
    "feedback_template": "A {{digits}}-digit decimal PIN code has 10^{{digits}} = {{expected}} total possible combinations."
  },
  {
    "type": "dynamic_numeric",
    "question": "An attacker attempts {{attempts_per_sec}} password guesses per second. How many total attempts occur in {{minutes}} minutes?",
    "variables": {
      "attempts_per_sec": {
        "min": 100,
        "max": 500,
        "step": 100
      },
      "minutes": {
        "min": 2,
        "max": 10,
        "step": 2
      }
    },
    "answer": {
      "expr": "attempts_per_sec * minutes * 60",
      "tolerance_abs": 0
    },
    "feedback_template": "At {{attempts_per_sec}} guesses a second, {{minutes}} minutes is {{seconds}} seconds, which allows {{expected}} attempts in total.",
    "derived": {
      "seconds": "minutes * 60"
    }
  },
  {
    "type": "dynamic_numeric",
    "question": "A password is exactly {{chars}} characters long, and each character is chosen from an alphabet of {{alpha}} possible symbols. How many different passwords are possible?",
    "variables": {
      "chars": {
        "min": 2,
        "max": 4
      },
      "alpha": {
        "values": [
          10,
          16,
          26,
          36
        ]
      }
    },
    "answer": {
      "expr": "pow(alpha, chars)",
      "tolerance_abs": 0
    },
    "feedback_template": "Each of the {{chars}} positions is chosen independently from {{alpha}} symbols, so the count is {{alpha}}^{{chars}} = {{expected}}. Adding one character multiplies the work for an attacker; adding one symbol to the alphabet barely helps."
  },
  {
    "question": "What is the principle of Defense in Depth in cybersecurity?",
    "correct": [
      "Layering multiple independent security controls so no single failure exposes the system"
    ],
    "incorrect": [
      "Concentrating the whole security budget on the strongest perimeter firewall available",
      "Adding a second security control after the first control has already been breached",
      "Choosing security controls that share one component so they can be audited together"
    ],
    "feedback": "Defense in depth uses overlapping, redundant security measures so that if one defensive layer fails, others continue to protect."
  },
  {
    "question": "What primary threat does a `.gitignore` file protect against when properly configured?",
    "correct": [
      "Accidentally committing secret API keys and private credentials into version control"
    ],
    "incorrect": [
      "Accidentally overwriting a teammate's committed changes when two branches are merged",
      "Accidentally publishing a repository publicly before a security review has finished",
      "Accidentally leaving a secret readable in an earlier commit after the line is deleted"
    ],
    "feedback": "A `.gitignore` file ensures local files containing secrets (like `.env`) are ignored by git and not published."
  },
  {
    "question": "What is a brute-force attack, and which simple measure blunts it?",
    "correct": [
      "Trying guess after guess; locking the account after failures"
    ],
    "incorrect": [
      "Flooding a server with traffic; adding more server capacity",
      "Guessing a database's table names; renaming the tables",
      "Reading packets on a network; switching to a different Wi-Fi"
    ],
    "feedback": "Slow hashing makes each guess costly, and a lockout rule turns millions of guesses a second into a handful per hour — without ever storing a readable password."
  },
  {
    "question": "Why is storing user passwords in clear, unhashed plaintext dangerous?",
    "correct": [
      "If the database is leaked, every user's password is stolen immediately"
    ],
    "incorrect": [
      "Anyone watching the network can read the password while it is in transit",
      "Users can no longer reset a forgotten password without asking an administrator",
      "The site cannot offer multi-factor authentication while passwords stay unhashed"
    ],
    "feedback": "Plaintext passwords expose all user accounts if data is compromised; salted hashing ensures passwords cannot be read directly."
  },
  {
    "question": "You click \"forgot password\" and the site emails you your existing password in readable text. What does that tell you about how it stores passwords?",
    "correct": [
      "The site can recover the original text, so it is not storing one-way hashes"
    ],
    "incorrect": [
      "The site salted its hashes, and the salt is what makes the original recoverable again",
      "The site hashed the password and simply ran the same hash algorithm backwards",
      "The site stores only hashes, and what arrived in the email was the stored hash"
    ],
    "feedback": "A correctly hashed password cannot be turned back into text by anyone, including the site, which is why safe sites reset a password instead of sending it. Salting strengthens hashes rather than undoing them, and a hash you could reverse would not be a hash."
  },
  {
    "type": "fill_blank",
    "question": "Requiring a second proof of identity, such as a code from the user's phone in addition to a password, is called ___-factor authentication.",
    "correct": [
      "two",
      "multi",
      "2"
    ],
    "feedback": "The point is that the two proofs fail in different ways: a stolen password is useless without the phone, so one leaked secret no longer hands over the account. It is defence in depth applied to logging in."
  },
  {
    "question": "A patient logs in over a coffee-shop Wi-Fi using plain HTTP. What is the risk?",
    "correct": [
      "Anyone on the network path can read the packets, password included"
    ],
    "incorrect": [
      "The password will be stored on the coffee shop's own server",
      "The login will fail because plain HTTP cannot carry a password",
      "The clinic's database will accept the login without checking it"
    ],
    "feedback": "Data is most exposed on the journey. The café router, the provider, and anyone snooping on the same Wi-Fi all sit on that path, and plain HTTP sends everything as readable text."
  },
  {
    "question": "A logged-in student edits the address bar from /records/1042 to /records/1043 and sees a classmate's record. Which control failed here?",
    "correct": [
      "Authorization: the server knew who was asking but never checked their permission"
    ],
    "incorrect": [
      "Authentication: the server never established which user was making the request",
      "Input validation: the record number in the URL was never checked to be a number",
      "Encryption: the record travelled over the network without any TLS protection"
    ],
    "feedback": "Log-in worked exactly as designed, so identity was established; what was missing was the per-record check that this identity may see this record. Note that 1043 is a perfectly valid number, so tighter type checking would have let the same request straight through."
  },
  {
    "question": "What is HTTPS?",
    "correct": [
      "HTTP communication encrypted using Transport Layer Security (TLS) certificates"
    ],
    "incorrect": [
      "A separate protocol that replaces HTTP with its own new set of request methods",
      "A rule that hides a site's IP address from the routers carrying its traffic",
      "A hashing scheme converting page content into a fixed-length digest in transit"
    ],
    "feedback": "HTTPS encrypts HTTP traffic using TLS/SSL certificates, protecting data confidentiality and integrity in transit."
  },
  {
    "question": "An attacker's page silently makes a logged-in user's browser submit a transfer form to their bank, and the bank accepts it because the browser attached the session cookie. What is this attack called?",
    "correct": [
      "Tricking a logged-in user's browser into unwanted actions on a trusted site"
    ],
    "incorrect": [
      "Injecting malicious SQL into database form fields to drop table schemas",
      "Intercepting and reading plain text packets sent over public cables",
      "Guessing user passwords through automated dictionary trial searches"
    ],
    "feedback": "CSRF exploits the trust a web app has in an authenticated user's browser, tricking it into submitting unauthorized commands."
  },
  {
    "question": "A padlock icon appears in the address bar of a scam website. What does the padlock actually promise?",
    "correct": [
      "That the connection is encrypted, not that the site is honest"
    ],
    "incorrect": [
      "That the site has been checked and approved as legitimate",
      "That the site does not collect any personal information",
      "That the site's owner has been identified by name"
    ],
    "feedback": "Certificates are easy to obtain, so a scammer can have a padlock too. 'This connection is private' and 'this site is trustworthy' are two very different claims."
  },
  {
    "question": "What fundamental rule should web developers follow regarding user-supplied input?",
    "correct": [
      "Never trust user input — validate and sanitize all incoming data before using it"
    ],
    "incorrect": [
      "Trust input coming from desktop users while validating input coming from mobile users",
      "Assume all input entered into text fields is already free of malicious code scripts",
      "Convert all user input directly into executable SQL queries to speed up server performance"
    ],
    "feedback": "The cardinal rule of web security is 'Never trust user input.' Validate, sanitize, escape, or parameterize all external data."
  },
  {
    "type": "fill_blank",
    "question": "The file used to prevent secret credentials from being tracked by git is ___.",
    "correct": [
      ".gitignore"
    ],
    "case_sensitive": true,
    "feedback": "The `.gitignore` file specifies untracked files that git should intentionally ignore."
  },
  {
    "type": "fill_blank",
    "question": "Values stored in the operating system environment to keep secrets out of code are called environment ___.",
    "correct": [
      "variables"
    ],
    "case_sensitive": false,
    "feedback": "Environment variables store configuration values and secrets outside source code files."
  },
  {
    "question": "Which of the following scenarios represent a breach of Availability? (Select all that apply)",
    "correct": [
      "A distributed denial-of-service (DDoS) attack taking a hospital website offline",
      "A server power outage crashing the database engine during operating hours"
    ],
    "incorrect": [
      "An unauthorized attacker reading private patient medical records in cleartext",
      "A malicious intruder altering a patient's stored allergy record in the database"
    ],
    "feedback": "Availability breaches prevent legitimate users from accessing operating services (e.g. outages or DDoS attacks)."
  },
  {
    "type": "cloze",
    "question": "Naming what a breach broke: quietly altering a lab result in a patient record breaks {{1}}, while flooding the hospital's server with junk traffic in a {{2}} attack breaks {{3}}.",
    "blanks": [
      {
        "accept": [
          "integrity"
        ],
        "hint": "CIA goal"
      },
      {
        "accept": [
          "DDoS",
          "DoS",
          "denial"
        ],
        "hint": "attack type"
      },
      {
        "accept": [
          "availability"
        ],
        "hint": "CIA goal"
      }
    ],
    "feedback": "The flood attack never reads or changes a single record, and the altered lab result is perfectly readable to everyone entitled to see it, so neither one is a confidentiality problem. Naming the goal that actually failed is what decides the right response."
  },
  {
    "question": "A developer commits a file containing an API key, then deletes the key in the very next commit. Why is the key still exposed?",
    "correct": [
      "Version control keeps every snapshot, so the old commit has the key"
    ],
    "incorrect": [
      "The key stays in the file until the repository is rebuilt",
      "Deleting a line does not remove it until the branch is merged",
      "The key is cached by the hosting site for exactly thirty days"
    ],
    "feedback": "The history is the point of git and it never forgets. The only safe response is to treat the key as compromised and revoke it, then issue a new one."
  },
  {
    "question": "Which of the following scenarios represent a breach of Integrity? (Select all that apply)",
    "correct": [
      "An unauthorized user modifying grade records stored in a university database",
      "A malicious script altering financial account balance figures during transit"
    ],
    "incorrect": [
      "An attacker taking down a web server by flooding it with millions of ping requests",
      "A user viewing their own account settings page after logging in successfully"
    ],
    "feedback": "Integrity breaches involve unauthorized modification, tampering, or corruption of data."
  },
  {
    "question": "Why is concatenating user input directly into a SQL query string dangerous?",
    "correct": [
      "The input can alter the query structure and execute unintended SQL commands"
    ],
    "incorrect": [
      "The extra characters make the query take far longer for the database to run",
      "The database returns results as text instead of the typed values code expects",
      "The input reaches the database before the user's session has been authenticated"
    ],
    "feedback": "Direct string concatenation allows user input containing SQL metacharacters (like `' OR '1'='1`) to alter query logic."
  },
  {
    "question": "What role does a digital certificate play in HTTPS connections?",
    "correct": [
      "Vouching for a server's identity and providing public keys for encrypted sessions"
    ],
    "incorrect": [
      "Confirming the site's owner is honest and that its content has been reviewed",
      "Storing the shared secret key both sides use to encrypt the rest of a session",
      "Recording which users have logged in so the server can authorize their requests"
    ],
    "feedback": "Certificates issued by trusted Certificate Authorities authenticate a server's domain identity and enable secure TLS encryption."
  },
  {
    "type": "matching",
    "question": "Match each security threat to its primary defense.",
    "pairs": [
      {
        "term": "SQL Injection",
        "definition": "Parameterized SQL queries with placeholder parameters"
      },
      {
        "term": "Cross-Site Scripting (XSS)",
        "definition": "HTML character escaping and strict input validation"
      },
      {
        "term": "Hardcoded Secrets",
        "definition": "Environment variables excluded via .gitignore"
      },
      {
        "term": "Eavesdropping in Transit",
        "definition": "HTTPS encryption using TLS digital certificates"
      }
    ],
    "feedback": "Parameterized queries stop SQLi, escaping stops XSS, environment variables protect secrets, and HTTPS stops eavesdropping."
  },
  {
    "question": "What is a threat model in software engineering?",
    "correct": [
      "A documentation of a system's assets, potential threat vectors, and risk mitigations"
    ],
    "incorrect": [
      "A record of the security incidents that have already happened to a live system",
      "A ranked list of software defects found by scanning source code for mistakes",
      "A signed promise to customers that the data they store will be kept private"
    ],
    "feedback": "Threat modeling identifies valuable assets, potential threat actors, vulnerabilities, and countermeasure plans."
  },
  {
    "type": "dynamic_numeric",
    "question": "A system uses a salt of {{salt_bytes}} bytes and a hash of {{hash_bytes}} bytes for each password. How many total bytes are stored per user record?",
    "variables": {
      "salt_bytes": {
        "min": 16,
        "max": 32,
        "step": 8
      },
      "hash_bytes": {
        "min": 32,
        "max": 64,
        "step": 16
      }
    },
    "answer": {
      "expr": "salt_bytes + hash_bytes",
      "tolerance_abs": 0
    },
    "feedback_template": "Adding {{salt_bytes}} salt bytes and {{hash_bytes}} hash bytes equals {{expected}} total stored bytes per user."
  },
  {
    "question": "What is the purpose of input validation in a secure application?",
    "correct": [
      "Checking that incoming data matches expected types, lengths, and formats before processing"
    ],
    "incorrect": [
      "Checking that a user's supplied credentials match the account they claim to own",
      "Checking that a logged-in user holds the permissions needed for a given action",
      "Checking that data stored earlier has not been altered since it was written down"
    ],
    "feedback": "Input validation verifies that data entering a system strictly conforms to expected specifications before processing."
  }
]

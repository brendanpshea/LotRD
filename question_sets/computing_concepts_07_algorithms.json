[
  {
    "question": "What does a linear search do?",
    "correct": ["Checks the items one by one until it finds the target or runs out"],
    "incorrect": [
      "Jumps straight to the middle item and stops there",
      "Sorts the list before looking at any of the items",
      "Splits the list in half and discards one half each step"
    ],
    "feedback": "A linear search walks through the list item by item. It works on any list but grows slower as the list grows."
  },
  {
    "question": "When is a linear search the only option?",
    "correct": ["When the list is unsorted, so faster search methods cannot be used"],
    "incorrect": [
      "When the list is already sorted from low to high",
      "When the list contains only numbers and no text",
      "When the list is short enough to fit on one screen"
    ],
    "feedback": "Faster searches like binary search require sorted data. If the list is unsorted, checking each item in turn is the only general option."
  },
  {
    "question": "What is the Big-O running time of a linear search over a list of n items?",
    "correct": ["O(n)"],
    "incorrect": ["O(log n)", "O(1)", "O(n²)"],
    "feedback": "In the worst case a linear search checks every item, so the work grows in proportion to n: O(n)."
  },
  {
    "question": "What must be true about a list before you can run a binary search on it?",
    "correct": ["The list must already be sorted"],
    "incorrect": [
      "The list must contain only whole numbers",
      "The list must have an even number of items",
      "The list must be stored as a tuple, not a list"
    ],
    "feedback": "Binary search relies on order: it decides which half to discard by comparing with the middle item. That only works if the list is sorted."
  },
  {
    "question": "How does a binary search find a target in a sorted list?",
    "correct": ["It checks the middle item and discards the half that cannot contain the target"],
    "incorrect": [
      "It checks each item from left to right in order",
      "It checks the first and last items and then stops",
      "It sorts the list again before every comparison"
    ],
    "feedback": "Binary search compares the target with the middle item and throws away the half it cannot be in, halving the search each step."
  },
  {
    "question": "What is the Big-O running time of a binary search?",
    "correct": ["O(log n)"],
    "incorrect": ["O(n)", "O(n log n)", "O(n²)"],
    "feedback": "Each step of a binary search halves the remaining items, so the number of steps grows like log n: O(log n)."
  },
  {
    "question": "In our search functions, what value signals 'not found'?",
    "correct": ["None, Python's built-in 'nothing here' value"],
    "incorrect": [
      "-1, the way many other languages do it",
      "0, since it is the first list index",
      "An empty list with no items in it"
    ],
    "feedback": "We return None as the sentinel for 'not found'. Avoid -1 in Python, because -1 is a valid index meaning the last item."
  },
  {
    "type": "fill_blank",
    "question": "A search that repeatedly halves a sorted list to find a target is called a ___ search.",
    "correct": ["binary"],
    "case_sensitive": false,
    "feedback": "Binary search halves the remaining items each step, giving O(log n) running time on sorted data."
  },
  {
    "question": "What does Big-O notation describe?",
    "correct": ["How an algorithm's work grows as the input size grows"],
    "incorrect": [
      "The exact number of seconds an algorithm takes to run",
      "How many lines of code an algorithm is written in",
      "The amount of memory a computer has installed"
    ],
    "feedback": "Big-O is about growth: it describes how the amount of work scales with input size n, ignoring exact times and constant factors."
  },
  {
    "question": "Order these growth rates from fastest-growing to slowest-growing for large n. Which grows the fastest?",
    "correct": ["O(n²)"],
    "incorrect": ["O(n log n)", "O(n)", "O(log n)"],
    "feedback": "For large n, O(n²) grows fastest, then O(n log n), then O(n), then O(log n). Slower growth means a more scalable algorithm."
  },
  {
    "question": "Which Big-O describes work that stays the same no matter how big the input is?",
    "correct": ["O(1)"],
    "incorrect": ["O(log n)", "O(n)", "O(n²)"],
    "feedback": "O(1) is constant time: the work does not change as n grows, like reading one item by its index."
  },
  {
    "type": "matching",
    "question": "Match each Big-O class to a description of how its work grows.",
    "pairs": [
      { "term": "O(1)", "definition": "Constant: the same work for any input size" },
      { "term": "O(log n)", "definition": "Grows slowly, like repeatedly halving the data" },
      { "term": "O(n)", "definition": "Grows in direct proportion to the input" },
      { "term": "O(n²)", "definition": "Grows with the square of the input" }
    ],
    "feedback": "These classes rank how fast work grows: constant, logarithmic, linear, and quadratic, from most to least scalable."
  },
  {
    "type": "matching",
    "question": "Match each algorithm to its typical Big-O running time.",
    "pairs": [
      { "term": "Linear search", "definition": "O(n)" },
      { "term": "Binary search", "definition": "O(log n)" },
      { "term": "Selection sort", "definition": "O(n²)" },
      { "term": "Merge sort", "definition": "O(n log n)" }
    ],
    "feedback": "Knowing an algorithm's Big-O lets you predict how it will scale before you ever run it on large data."
  },
  {
    "question": "How does selection sort work?",
    "correct": ["It repeatedly selects the smallest remaining item and moves it into place"],
    "incorrect": [
      "It splits the list in half and merges the sorted halves",
      "It checks the middle item and discards one half",
      "It swaps random pairs until the list happens to be sorted"
    ],
    "feedback": "Selection sort finds the smallest remaining item each pass and places it next. Its nested scanning makes it O(n²)."
  },
  {
    "question": "What is the Big-O running time of selection sort?",
    "correct": ["O(n²)"],
    "incorrect": ["O(n)", "O(log n)", "O(n log n)"],
    "feedback": "Selection sort scans the remaining items on every pass, so its work grows with the square of n: O(n²)."
  },
  {
    "question": "Merge sort is a divide-and-conquer algorithm. How does it sort a list?",
    "correct": ["It splits the list in half, sorts each half, then merges them back together"],
    "incorrect": [
      "It selects the smallest item on each pass through the list",
      "It compares the target with the middle item repeatedly",
      "It checks every item one at a time from left to right"
    ],
    "feedback": "Merge sort divides the list, sorts the halves, and merges them in order. This divide-and-conquer approach runs in O(n log n)."
  },
  {
    "question": "Why is merge sort (O(n log n)) usually preferred over selection sort (O(n²)) for large lists?",
    "correct": ["Its work grows much more slowly as the list gets large"],
    "incorrect": [
      "It does not need to compare any of the items",
      "It only works on lists that are already sorted",
      "It uses no extra memory of any kind at all"
    ],
    "feedback": "For large n, O(n log n) grows far slower than O(n²), so merge sort scales to big inputs that would make selection sort painfully slow."
  },
  {
    "type": "fill_blank",
    "question": "The simple O(n²) sort that repeatedly picks the smallest remaining item is called ___ sort.",
    "correct": ["selection"],
    "case_sensitive": false,
    "feedback": "Selection sort selects the smallest remaining item each pass. It is easy to write but slow on large lists."
  },
  {
    "question": "What does Python's built-in  sorted(nums)  return?",
    "correct": ["A new sorted list, leaving the original unchanged"],
    "incorrect": [
      "Nothing; it sorts nums in place and returns None",
      "The single smallest value found in nums",
      "The original list reversed end to end"
    ],
    "feedback": "sorted() returns a brand-new sorted list and leaves the original alone. By contrast, nums.sort() rearranges nums in place."
  },
  {
    "question": "What is the difference between  nums.sort()  and  sorted(nums)?",
    "correct": [".sort() changes the list in place; sorted() returns a new sorted list"],
    "incorrect": [
      ".sort() returns a new list; sorted() changes it in place",
      "They behave identically and can be used interchangeably",
      ".sort() works on numbers only; sorted() works on text only"
    ],
    "feedback": "nums.sort() rearranges nums itself and returns None. sorted(nums) leaves nums alone and hands back a new sorted list."
  },
  {
    "question": "What does the  key  function do in  sorted(words, key=len)?",
    "correct": ["It tells Python to sort by each item's length instead of its value"],
    "incorrect": [
      "It removes any items whose length is too long",
      "It reverses the final order of the sorted list",
      "It limits the result to the first len items"
    ],
    "feedback": "A key function returns the value to sort by. key=len sorts the words by length rather than alphabetically."
  },
  {
    "type": "code_line",
    "question": "Given a list named nums, write the statement that sorts it in place (modifying nums itself).",
    "language": "python",
    "correct": ["nums.sort()"],
    "case_sensitive": true,
    "feedback": "nums.sort() rearranges nums in place and returns None."
  },
  {
    "type": "code_line",
    "question": "Given a list named nums, write the expression that returns a new sorted list without changing nums.",
    "language": "python",
    "correct": ["sorted(nums)"],
    "case_sensitive": true,
    "feedback": "sorted(nums) builds and returns a new sorted list, leaving nums unchanged."
  },
  {
    "type": "code_line",
    "question": "Write the one-line Python statement that swaps the values of two variables a and b.",
    "language": "python",
    "correct": ["a, b = b, a", "b, a = a, b"],
    "case_sensitive": true,
    "feedback": "a, b = b, a swaps the two values in a single statement using tuple unpacking."
  },
  {
    "type": "fill_blank",
    "question": "Rearranging a list without making a new copy is described as sorting ___ (two words joined by a hyphen).",
    "correct": ["in-place", "in place"],
    "case_sensitive": false,
    "feedback": "An in-place sort like nums.sort() rearranges the existing list rather than building a new one."
  },
  {
    "question": "What is a Turing machine?",
    "correct": ["A minimal tape-and-rulebook thought experiment that models computation"],
    "incorrect": [
      "The first electronic computer ever physically built",
      "A modern programming language for writing algorithms",
      "A device that can solve any problem instantly"
    ],
    "feedback": "A Turing machine is an imagined device with an endless tape and a simple rulebook. It is a model used to reason about what computation can do."
  },
  {
    "question": "What does it mean for a problem to be computable?",
    "correct": ["There is some algorithm that can solve it"],
    "incorrect": [
      "It can be solved instantly with no work at all",
      "It can be checked but never actually solved",
      "It can be solved only by a quantum computer"
    ],
    "feedback": "A problem is computable if some algorithm exists that solves it. Computability is about whether a method exists, not how fast it is."
  },
  {
    "question": "What does the Church-Turing thesis claim?",
    "correct": ["Anything computable can be computed by a Turing machine"],
    "incorrect": [
      "Every problem can be solved given enough time",
      "Turing machines are faster than real computers",
      "No problem can ever truly be computed"
    ],
    "feedback": "The Church-Turing thesis says the Turing machine captures exactly what 'computable' means: if any method can compute it, a Turing machine can too."
  },
  {
    "question": "What is the halting problem?",
    "correct": ["Deciding whether an arbitrary program will eventually stop or run forever"],
    "incorrect": [
      "Deciding which sorting algorithm is the fastest",
      "Stopping a program that is taking too long to run",
      "Finding the smallest input that makes a program crash"
    ],
    "feedback": "The halting problem asks whether a given program, on a given input, will ever halt. Turing proved no algorithm can answer this for every program."
  },
  {
    "question": "What does it mean that the halting problem is undecidable?",
    "correct": ["No algorithm can solve every case of it, even in principle"],
    "incorrect": [
      "It can be solved but only very slowly",
      "It can be solved only by powerful enough hardware",
      "It has simply not been solved by anyone yet"
    ],
    "feedback": "Undecidable means the impossibility is permanent: no program, however clever or fast, can correctly decide every case of the halting problem."
  },
  {
    "type": "fill_blank",
    "question": "A problem that no algorithm can solve in every case, like the halting problem, is called ___.",
    "correct": ["undecidable"],
    "case_sensitive": false,
    "feedback": "Undecidable problems are beyond any algorithm in principle, not just slow or unsolved."
  },
  {
    "question": "In complexity theory, what is the class P?",
    "correct": ["Problems that can be solved quickly by an algorithm"],
    "incorrect": [
      "Problems whose answers can be checked quickly",
      "Problems that no algorithm can ever solve",
      "Problems that require a Turing machine to state"
    ],
    "feedback": "P is the class of problems an algorithm can solve quickly (in polynomial time). It is about solving, not just checking."
  },
  {
    "question": "In complexity theory, what defines the class NP?",
    "correct": ["Problems whose proposed answers can be checked quickly"],
    "incorrect": [
      "Problems that a fast algorithm can solve directly",
      "Problems that no algorithm is able to solve at all",
      "Problems that have no possible candidate answers"
    ],
    "feedback": "NP problems are easy to check: given a candidate answer, you can verify it quickly, even if finding the answer seems hard."
  },
  {
    "question": "The P vs. NP question is often summarized as which cultural idea?",
    "correct": ["Is every problem that is easy to check also easy to solve?"],
    "incorrect": [
      "Is every problem eventually solvable given enough time?",
      "Are computers faster than the people who program them?",
      "Can a Turing machine compute anything a person can?"
    ],
    "feedback": "P vs. NP asks whether 'easy to check' (NP) implies 'easy to solve' (P). It is a famous open question with no proven answer."
  },
  {
    "question": "What does it mean to call a problem intractable?",
    "correct": ["It is solvable in principle but needs impractically much work"],
    "incorrect": [
      "It cannot be solved by any algorithm at all",
      "It can be solved instantly on any computer",
      "It has never been written down clearly enough"
    ],
    "feedback": "Intractable problems have algorithms, but those algorithms need so much time on large inputs that they are useless in practice. This differs from undecidable, which means no algorithm exists."
  },
  {
    "question": "Which of the following statements about the limits of computation are correct? (Select all that apply)",
    "correct": [
      "The halting problem cannot be solved by any program, including AI",
      "Some problems are solvable in principle but intractable in practice",
      "Being easy to check does not guarantee a problem is easy to solve"
    ],
    "incorrect": [
      "A fast enough computer could eventually solve the halting problem",
      "Every problem in NP is already known to be in P"
    ],
    "feedback": "Undecidable problems like halting are impossible for any program, intractable problems are merely impractical, and whether NP equals P is unknown. No amount of speed crosses the undecidable wall."
  },
  {
    "question": "Why can't a more powerful AI or LLM solve an undecidable problem like the halting problem?",
    "correct": ["The limit is mathematical, so no program of any kind can cross it"],
    "incorrect": [
      "AI simply has not been trained on enough examples yet",
      "AI runs on hardware that is currently too slow",
      "AI can solve it, but only one case at a time"
    ],
    "feedback": "Undecidability is a proven mathematical limit on algorithms themselves. An AI is still a program, so it is bound by the same wall, no matter how capable it becomes."
  },
  {
    "type": "dynamic_numeric",
    "question": "In the worst case, how many comparisons does a linear search make on a list of {{n}} items?",
    "variables": {
      "n": { "min": 6, "max": 40 }
    },
    "answer": {
      "expr": "n",
      "tolerance_abs": 0
    },
    "feedback_template": "A linear search may have to check every item, so the worst case on {{n}} items is {{expected}} comparisons."
  },
  {
    "type": "dynamic_numeric",
    "question": "A sorted list has {{n}} items. At most how many comparisons does a binary search make? (This is floor(log2 n) + 1.)",
    "variables": {
      "n": { "min": 8, "max": 250 }
    },
    "answer": {
      "expr": "bitLength(n)",
      "tolerance_abs": 0
    },
    "feedback_template": "Binary search halves the list each step, so at most floor(log2 {{n}}) + 1 = {{expected}} comparisons are needed."
  },
  {
    "type": "dynamic_numeric",
    "question": "Selection sort compares every pair of items once. For a list of {{n}} items, that is n*(n-1)/2 comparisons. How many is that?",
    "variables": {
      "n": { "min": 4, "max": 12 }
    },
    "answer": {
      "expr": "n * (n - 1) / 2",
      "tolerance_abs": 0
    },
    "feedback_template": "With {{n}} items, selection sort makes {{n}}*({{n}}-1)/2 = {{expected}} comparisons, which is why it is O(n squared)."
  }
]

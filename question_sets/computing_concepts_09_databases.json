[
  {
    "question": "What is the primary advantage of storing application data in a database rather than in plain files?",
    "correct": [
      "Databases enforce integrity rules and handle concurrent access safely"
    ],
    "incorrect": [
      "Databases require less storage space on disk than plain text files do",
      "Databases convert all stored data into executable Python code files",
      "Databases eliminate the need for backup copies or security settings"
    ],
    "feedback": "Databases provide permanence, enforce rules through integrity constraints, and manage concurrent access securely."
  },
  {
    "question": "In the relational database model, what does a table represent?",
    "correct": [
      "A named grid containing records of one specific entity type"
    ],
    "incorrect": [
      "A temporary cache memory buffer cleared when a program stops",
      "A file format that stores arbitrary unstructured program logs",
      "A collection of functions used to build dynamic web pages"
    ],
    "feedback": "In a relational model, each table represents a single entity type (like customers or orders) arranged in rows and columns."
  },
  {
    "question": "What is the purpose of a primary key in a relational database table?",
    "correct": [
      "Uniquely identifying each individual row in a table permanently"
    ],
    "incorrect": [
      "Encrypting sensitive column values stored inside the database grid",
      "Sorting query results automatically in alphabetical order on read",
      "Linking two separate database files across different computer servers"
    ],
    "feedback": "A primary key is a column or set of columns whose value uniquely and permanently identifies each row in its table."
  },
  {
    "question": "What role does a foreign key play in a relational database?",
    "correct": [
      "Storing the primary key of a row in another table to link them"
    ],
    "incorrect": [
      "Providing external administrator access passwords for a database",
      "Translating SQL query keywords into foreign language strings",
      "Holding temporary calculations generated by database functions"
    ],
    "feedback": "A foreign key links two tables by referencing the primary key of another table, creating a relational link."
  },
  {
    "question": "Which SQL statement is used to fetch and filter records from a database table?",
    "correct": [
      "SELECT"
    ],
    "incorrect": [
      "FETCH",
      "GET",
      "READ"
    ],
    "feedback": "The SELECT statement is used in SQL to query data from database tables."
  },
  {
    "type": "fill_blank",
    "question": "The SQL keyword used to filter rows matching a specific condition is ___.",
    "correct": [
      "WHERE"
    ],
    "case_sensitive": false,
    "feedback": "The WHERE clause filters rows in a SELECT, UPDATE, or DELETE query based on logical conditions."
  },
  {
    "type": "fill_blank",
    "question": "The SQL clause used to combine rows from two tables based on matching key values is ___.",
    "correct": [
      "JOIN",
      "INNER JOIN"
    ],
    "case_sensitive": false,
    "feedback": "JOIN recombines related rows from multiple tables using matching key columns."
  },
  {
    "type": "fill_blank",
    "question": "The SQL command used to insert a new row of data into a database table is ___.",
    "correct": [
      "INSERT",
      "INSERT INTO"
    ],
    "case_sensitive": false,
    "feedback": "INSERT (or INSERT INTO) adds new records into an existing table."
  },
  {
    "question": "Which of the following are Data Manipulation Language (DML) commands in SQL? (Select all that apply)",
    "correct": [
      "SELECT",
      "INSERT",
      "UPDATE"
    ],
    "incorrect": [
      "CREATE TABLE",
      "DROP TABLE",
      "ALTER TABLE"
    ],
    "feedback": "DML commands (SELECT, INSERT, UPDATE, DELETE) operate on data rows. DDL commands (CREATE, DROP, ALTER) modify structure."
  },
  {
    "question": "Which of the following are Data Definition Language (DDL) commands in SQL? (Select all that apply)",
    "correct": [
      "CREATE TABLE",
      "DROP TABLE"
    ],
    "incorrect": [
      "SELECT",
      "INSERT",
      "UPDATE"
    ],
    "feedback": "DDL commands define or change table schemas (CREATE, DROP, ALTER), whereas DML statements manipulate actual rows."
  },
  {
    "question": "Which of the following are SQL aggregate functions? (Select all that apply)",
    "correct": [
      "SUM()",
      "COUNT()",
      "AVG()"
    ],
    "incorrect": [
      "JOIN()",
      "WHERE()",
      "FILTER()"
    ],
    "feedback": "SUM(), COUNT(), AVG(), MIN(), and MAX() are SQL aggregate functions that collapse multiple rows into a single value."
  },
  {
    "question": "Which SQL clause is paired with aggregate functions to summarize rows by category? (Select all that apply)",
    "correct": [
      "GROUP BY"
    ],
    "incorrect": [
      "ORDER BY",
      "WHERE",
      "LIMIT"
    ],
    "feedback": "GROUP BY partitions rows into summary groups so aggregate functions compute values for each category separately."
  },
  {
    "type": "matching",
    "question": "Match each SQL command or clause to its primary purpose.",
    "pairs": [
      {
        "term": "SELECT",
        "definition": "Retrieves specified columns and rows from one or more tables"
      },
      {
        "term": "INSERT",
        "definition": "Adds new records with column values into a targeted table"
      },
      {
        "term": "UPDATE",
        "definition": "Modifies existing column values in rows matching a condition"
      },
      {
        "term": "DELETE",
        "definition": "Removes rows matching a specified condition from a table"
      }
    ],
    "feedback": "SELECT reads data, INSERT creates rows, UPDATE modifies rows, and DELETE removes rows in SQL."
  },
  {
    "type": "matching",
    "question": "Match each database concept to its definition.",
    "pairs": [
      {
        "term": "Primary Key",
        "definition": "A column value that uniquely identifies each record in a table"
      },
      {
        "term": "Foreign Key",
        "definition": "A column pointing to the primary key of another table"
      },
      {
        "term": "Integrity Constraint",
        "definition": "A rule enforced by the DBMS to prevent bad or missing data"
      },
      {
        "term": "DBMS",
        "definition": "The software system managing stored tables and SQL execution"
      }
    ],
    "feedback": "Primary and foreign keys establish structure and relationships, integrity constraints safeguard accuracy, and the DBMS manages execution."
  },
  {
    "type": "dynamic_numeric",
    "question": "A query returns {{rows}} rows per page. How many total records are retrieved across {{pages}} full pages?",
    "variables": {
      "rows": {
        "min": 10,
        "max": 50,
        "step": 5
      },
      "pages": {
        "min": 2,
        "max": 8
      }
    },
    "answer": {
      "expr": "rows * pages",
      "tolerance_abs": 0
    },
    "feedback_template": "Multiplying {{rows}} records per page by {{pages}} pages equals {{expected}} total records."
  },
  {
    "type": "dynamic_numeric",
    "question": "A table has {{initial}} rows. After inserting {{inserted}} rows and deleting {{deleted}} rows, how many rows remain?",
    "variables": {
      "initial": {
        "min": 100,
        "max": 500,
        "step": 50
      },
      "inserted": {
        "min": 10,
        "max": 40
      },
      "deleted": {
        "min": 5,
        "max": 25
      }
    },
    "answer": {
      "expr": "initial + inserted - deleted",
      "tolerance_abs": 0
    },
    "feedback_template": "Starting with {{initial}}, adding {{inserted}}, and removing {{deleted}} leaves {{expected}} rows."
  },
  {
    "question": "What is an integrity constraint in a relational database?",
    "correct": [
      "A schema rule enforced by the database that rejects invalid changes"
    ],
    "incorrect": [
      "A script that runs periodic backups of table files to cloud drives",
      "A network firewall rule restricting SQL connection port numbers",
      "A method for speeding up queries by creating index files on disk"
    ],
    "feedback": "Integrity constraints (such as foreign key checks or NOT NULL rules) prevent invalid or corrupt data from being inserted."
  },
  {
    "question": "Why is a database refusing an invalid operation considered a feature rather than a flaw?",
    "correct": [
      "It guarantees that invalid or corrupted data cannot enter the system"
    ],
    "incorrect": [
      "It allows developers to skip writing SQL error handling code completely",
      "It increases server memory usage to slow down malicious user requests",
      "It automatically deletes previous data backups when an error happens"
    ],
    "feedback": "A DBMS rejecting invalid operations prevents bad data states that could break applications or corrupt business records."
  },
  {
    "question": "How does a document (NoSQL) database differ from a relational database?",
    "correct": [
      "Document databases store flexible JSON records without strict table schemas"
    ],
    "incorrect": [
      "Document databases forbid using text strings and store only numbers",
      "Document databases run exclusively inside offline spreadsheet software",
      "Document databases require foreign keys for every stored data field"
    ],
    "feedback": "Document databases store nested semi-structured items (like JSON), trading relational constraints for schema flexibility."
  },
  {
    "question": "In which scenario is a document (NoSQL) database typically preferred over a relational database?",
    "correct": [
      "When stored items have rapidly changing shapes and varied attributes"
    ],
    "incorrect": [
      "When strict cross-table transactional safety is the top priority",
      "When every record fits neatly into identical fixed-length columns",
      "When complex multi-table SQL joins are performed for every query"
    ],
    "feedback": "Document databases excel when data structures evolve rapidly or records vary significantly in their attributes."
  },
  {
    "question": "Which text format is commonly used to represent records in document databases?",
    "correct": [
      "JSON"
    ],
    "incorrect": [
      "CSV",
      "HTML",
      "BYTE"
    ],
    "feedback": "JSON (JavaScript Object Notation) is the standard format for representing nested objects in document databases."
  },
  {
    "type": "fill_blank",
    "question": "A single entry or record stored horizontally in a database table is called a ___.",
    "correct": [
      "row",
      "tuple",
      "record"
    ],
    "case_sensitive": false,
    "feedback": "A row (or record/tuple) represents one single instance of an item in a table grid."
  },
  {
    "type": "fill_blank",
    "question": "A vertical attribute representing one fact about every row in a table is called a ___.",
    "correct": [
      "column",
      "field",
      "attribute"
    ],
    "case_sensitive": false,
    "feedback": "A column (or field) defines one specific piece of data recorded for every row in a table."
  },
  {
    "question": "What is the purpose of the SQL ORDER BY clause?",
    "correct": [
      "Sorting returned rows in ascending or descending sequence"
    ],
    "incorrect": [
      "Grouping rows into summary rows for mathematical aggregation",
      "Filtering out rows before any query processing takes place",
      "Joining separate table columns together based on primary keys"
    ],
    "feedback": "ORDER BY sorts query results according to one or more specified columns (ASC or DESC)."
  },
  {
    "question": "Which SQL statement is used to create a new database table and define its columns?",
    "correct": [
      "CREATE TABLE"
    ],
    "incorrect": [
      "NEW TABLE",
      "MAKE TABLE",
      "BUILD TABLE"
    ],
    "feedback": "CREATE TABLE is the DDL command used to establish a new table and specify column data types and constraints."
  },
  {
    "question": "Which of the following describes a key feature of relational database tables? (Select all that apply)",
    "correct": [
      "Every column in a table has a designated data type",
      "Each row is uniquely identified by a primary key",
      "Tables can be linked together using foreign keys"
    ],
    "incorrect": [
      "Rows in a table can contain arbitrary unlabelled text without columns",
      "Primary keys are allowed to contain duplicate values across rows"
    ],
    "feedback": "Relational tables enforce column types, require unique primary keys, and establish relationships via foreign keys."
  },
  {
    "question": "Which of the following are valid reasons to use a relational DBMS instead of spreadsheets? (Select all that apply)",
    "correct": [
      "Enforcing foreign key references between related entities",
      "Handling thousands of concurrent multi-user transactions safely"
    ],
    "incorrect": [
      "Allowing any user to corrupt column types without error alerts",
      "Storing unstructured image files directly inside integer columns"
    ],
    "feedback": "Relational databases excel at enforcing data integrity and supporting safe concurrent multi-user transactions."
  },
  {
    "question": "What occurs when a SQL query uses an aggregate function like COUNT() without a GROUP BY clause?",
    "correct": [
      "The aggregate function operates on all matching rows and returns one summary value"
    ],
    "incorrect": [
      "The database throws a syntax error and cancels the query execution immediately",
      "The query returns separate summary values for every individual column in the table",
      "The database duplicates every row in the table before performing the aggregate sum"
    ],
    "feedback": "Without GROUP BY, aggregate functions treat all matching rows in the query result set as a single group."
  },
  {
    "type": "matching",
    "question": "Match each database model type to its characteristic.",
    "pairs": [
      {
        "term": "Relational Database",
        "definition": "Organizes data into fixed tables linked by primary and foreign keys"
      },
      {
        "term": "Document Database",
        "definition": "Stores semi-structured JSON objects with flexible schema fields"
      },
      {
        "term": "Spreadsheet",
        "definition": "Provides visual cell grids but lacks strict multi-user integrity rules"
      },
      {
        "term": "Plain Text File",
        "definition": "Stores unparsed raw text lines without built-in query indexing"
      }
    ],
    "feedback": "Relational databases use tables, document databases store JSON, spreadsheets lack strict rules, and plain files lack query indexing."
  },
  {
    "question": "What is SQLite?",
    "correct": [
      "A lightweight serverless relational database engine stored in a single file"
    ],
    "incorrect": [
      "A cloud-only NoSQL document storage system developed for big data processing",
      "A web browser extension used for styling HTML elements with CSS variables",
      "A Python library designed exclusively for plotting scientific chart graphics"
    ],
    "feedback": "SQLite is a compact, self-contained, serverless relational database engine used widely in applications."
  },
  {
    "type": "dynamic_numeric",
    "question": "A table has {{total}} records. A WHERE clause filters out {{filtered}} records. How many records are returned?",
    "variables": {
      "total": {
        "min": 80,
        "max": 200,
        "step": 20
      },
      "filtered": {
        "min": 15,
        "max": 50,
        "step": 5
      }
    },
    "answer": {
      "expr": "total - filtered",
      "tolerance_abs": 0
    },
    "feedback_template": "Subtracting {{filtered}} filtered records from {{total}} total records yields {{expected}} returned records."
  },
  {
    "question": "Which SQL clause is used to eliminate duplicate rows from a query result set?",
    "correct": [
      "DISTINCT"
    ],
    "incorrect": [
      "UNIQUE",
      "REMOVE",
      "FILTER"
    ],
    "feedback": "The SELECT DISTINCT statement is used to return only distinct (different) values, removing duplicate rows."
  }
]

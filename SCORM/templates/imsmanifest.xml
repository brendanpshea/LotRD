<?xml version="1.0" encoding="UTF-8"?>
<manifest identifier="{{IDENTIFIER}}" version="1.0"
  xmlns="http://www.imsproject.org/xsd/imscp_rootv1p1p2"
  xmlns:adlcp="http://www.adlnet.org/xsd/adlcp_rootv1p2"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://www.imsproject.org/xsd/imscp_rootv1p1p2 imscp_rootv1p1p2.xsd
                      http://www.imsproject.org/xsd/imsmd_rootv1p2p1 imsmd_rootv1p2p1.xsd
                      http://www.adlnet.org/xsd/adlcp_rootv1p2 adlcp_rootv1p2.xsd">
  <metadata>
    <schema>ADL SCORM</schema>
    <schemaversion>1.2</schemaversion>
  </metadata>
  <organizations default="ORG-1">
    <organization identifier="ORG-1">
      <title>{{TITLE}}</title>
      <!-- Deliberately no mastery score. Under SCORM 1.2 the LMS compares
           it against cmi.core.score.raw and may stamp the attempt passed/failed
           on its own. Credit here accrues over weeks — a student sitting at 80%
           in week 3 is on track, not failing — so a mastery threshold would show
           most of the class as failed for most of the term. Completion is
           reported explicitly via cmi.core.lesson_status instead. -->
      <item identifier="ITEM-1" identifierref="RES-1">
        <title>{{TITLE}}</title>
      </item>
    </organization>
  </organizations>
  <resources>
    <resource identifier="RES-1" type="webcontent" adlcp:scormtype="sco" href="index.html">
{{FILE_LIST}}
    </resource>
  </resources>
</manifest>
